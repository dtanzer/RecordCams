<UserControl
    x:Class="RecordCams.RecorderControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecordCams"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="ProjectName" PlaceholderText="Project Name" Grid.Column="0" Text="{x:Bind ProjectNameText, Mode=TwoWay}" />
        <Button x:Name="RecordStop" Content="Record" Click="RecordButtonClicked" Grid.Column="1">
            <Button.KeyboardAccelerators>
                <KeyboardAccelerator Key="PageDown" />
            </Button.KeyboardAccelerators>
        </Button>
        <Button x:Name="Cancel" Content="Cancel" Click="CancelButtonClicked" Grid.Column="2" Visibility="Collapsed">
            <Button.KeyboardAccelerators>
                <KeyboardAccelerator Key="PageUp" />
            </Button.KeyboardAccelerators>
        </Button>
    </Grid>
</UserControl>
